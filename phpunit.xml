<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <ini name="memory_limit" value="512M"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <!-- Canal de SISTEMA (fundação v0.7.0): convite, link de senha e troca de e-mail
             saem pelo mailer `system`, declarado em config/mail.php. Na suíte ele usa o
             transporte `array` (é o que Mail::fake/assertQueued lê) e um remetente do
             subdomínio de sistema, para os testes medirem a postura real. -->
        <env name="HELIX_SYSTEM_MAIL_TRANSPORT" value="array"/>
        <env name="HELIX_SYSTEM_MAIL_FROM_ADDRESS" value="no-reply@sistema.helix.test"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <!-- Tenancy fail-closed (auditoria multitenant P2): consulta/criação sem tenant no contexto lança. -->
        <env name="HELIX_TENANCY_STRICT" value="true"/>
        <!-- .env.example declara TWO_FACTOR_ENFORCE=true (postura de produção, exigida
             pelo helix:doctor). Na suíte o 2FA obrigatório desviaria os testes de tela
             para o setup (302); o comportamento testado é o de APP_ENV=testing. -->
        <env name="TWO_FACTOR_ENFORCE" value="false"/>
    </php>
</phpunit>
